[project]
name = "karyo-backend"
version = "0.1.0"
description = "Backend API for Karyo"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn[standard]>=0.40.0",
]
